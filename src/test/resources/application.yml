spring:
  config:
    import: "classpath:application.yml"  # main의 설정을 가져옴
  profiles:
    active: test

  mail:
    host: dwqfwq
    port: 587
    username: fqwfwq
    password: fwqfwq

  jackson:
    generator:
      write-numbers-as-strings: true


  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    #    database-platform: org.hibernate.spatial.dialect.postgis.PostgisDialect
    properties:
      hibernate:
        format_sql: false
        show_sql: false
  #        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test

jwt:
  access-token-secret: ioconw
  refresh-token-secret: fewqgfqww
  oauth-verification-token-secret: 1241412fwqqw
  email-verification-token-secret: WQFEQ
  access-token-validity-in-seconds: 3600
  refresh-token-validity-in-seconds: 604800
  verification-token-validity-in-seconds: 600


management:
  health:
    mail:
      enabled: false
  server:
    port: 9090

logging:
  level:
    root: WARN
    org.testcontainers: INFO
  org:
    testcontainers:
      level: DEBUG

app:
  base-url: ${APP_BASE_URL:http://localhost:${SERVER_PORT:8080}}
  firebase-configuration: |
    {
        "type": "service_account",
        "project_id": "test-project",
        "private_key_id": "test-private
    }
