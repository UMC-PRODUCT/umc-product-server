global:
  scrape_interval: 30s

scrape_configs:
  # OTel Collector가 노출하는 Prometheus 엔드포인트에서 메트릭 수집
  # (Spring Boot → OTel Collector → 여기서 scrape)
  - job_name: 'otel-collector'
    static_configs:
      - targets: [ 'otel-collector:8889' ]
